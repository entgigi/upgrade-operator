---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: entandoupgrades.upgrade.entando.org
spec:
  group: upgrade.entando.org
  names:
    kind: EntandoUpgrade
    listKind: EntandoUpgradeList
    plural: entandoupgrades
    singular: entandoupgrade
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EntandoUpgrade is the Schema for the entandoupgrades API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EntandoUpgradeSpec defines the desired state of EntandoUpgrade
            properties:
              fromVersion:
                description: FromVersion is an example field of EntandoUpgrade. Edit
                  entandoupgrade_types.go to remove/update
                type: string
              toVersion:
                description: ToVersion is an example field of EntandoUpgrade. Edit
                  entandoupgrade_types.go to remove/update
                type: string
            required:
            - fromVersion
            - toVersion
            type: object
          status:
            description: EntandoUpgradeStatus defines the observed state of EntandoUpgrade
            properties:
              patch:
                type: string
              progress:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
